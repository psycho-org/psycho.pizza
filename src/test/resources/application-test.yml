spring:
  datasource:
    url: jdbc:h2:mem:psycho;MODE=PostgreSQL;NON_KEYWORDS=VALUE;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
  mail:
    host: smtp.gmail.com
    port: 587
    username: test@psycho.pizza
    password: test-password
  cloud:
    aws:
      region:
        static: ap-northeast-2
      credentials:
        access-key: test-dummy-access-key-id
        secret-key: test-dummy-secret-access-key

security:
  jwt:
    secret: ${JWT_SECRET:TEST-kYNFuADL-ekw9P+A6-qJdo5HKx-HejUHqD0-40Qi/chV-6a5W+FoP-MrbjWku4-G+jbT8HO-Pap5ojs2i}

challenge:
  otp-length: 6
  otp-ttl-seconds: 300
  otp-max-attempts: 3
  cooldown-seconds: 60
  confirmation-token-ttl-seconds: 300

openai:
  api-key: test-dummy-api-key
  model: gpt-test-model

sqs:
  request-queue-name: ${REQUEST_QUEUE_NAME:psycho-sqs-request-prod}
  response-queue-name: ${RESPONSE_QUEUE_NAME:psycho-sqs-response-prod}
