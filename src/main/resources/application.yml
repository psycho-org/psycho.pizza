spring:
  config:
    import: optional:file:.env[.properties]
  profiles:
    default: ${SPRING_PROFILES_ACTIVE}

management:
  endpoints:
    web:
      exposure:
        include: health

openai:
  api-key: ${OPENAI_API_KEY}
  model: gpt-4.1-mini