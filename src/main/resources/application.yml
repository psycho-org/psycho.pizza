spring:
  config:
    import: optional:file:.env[.properties]
  profiles:
    default: ${SPRING_PROFILES_ACTIVE}
  flyway:
    placeholder-replacement: false

management:
  endpoints:
    web:
      exposure:
        include: health
